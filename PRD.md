# 个人开销记账 APP 产品需求文档（PRD）

> 版本：v1.0  
> 日期：2026-09-07  
> 状态：已确认，作为第一版开发依据

---

## 1. 产品目标和使用场景

### 1.1 产品定位
一款**只供开发者本人使用**的本地个人开销记账 APP。无登录、无联网、无云数据库、无服务器端，所有数据存储在手机本地。

### 1.2 目标用户
- 唯一用户：开发者本人
- 使用设备：安卓手机
- 安装方式：APK 本地安装，不经过应用商店

### 1.3 核心使用场景
1. **快速记账**：消费后打开 APP，在聊天框输入一句话（如"吃饭 25 元"），3 秒完成记账。
2. **查看开销**：随时查看今日开销、本月预算使用情况、历史账单、分类统计。
3. **趣味驱动**：通过虚拟宠物养成、成就徽章、连续打卡等趣味功能，让记账变成日常习惯而非负担。
4. **预算管控**：月度预算 1200 元，五级提醒帮助控制消费，超支时给出分析建议。

### 1.4 设计理念
> 让用户每天打开 App 不是因为"我得记账了"，而是"我想看看我的小猫今天怎么样了"——在和宠物互动的过程中顺手把账记了。

### 1.5 技术形态
- **打包方式**：Capacitor（Web 技术打包为原生安卓 APP）
- **交付物**：APK 文件
- **Web 层**：单个 HTML 文件（内联 CSS、JS），放在 Capacitor 项目的 www 目录
- **数据存储**：LocalStorage（浏览器 WebView 本地存储）
- **运行方式**：离线可用，全屏运行，添加到桌面后独立图标启动
- **移动端适配**：最大宽度 520px，底部固定导航栏

### 1.6 视觉风格
- **主题**：温暖治愈 · 奶油橘猫风
- **页面背景**：奶油白 `#FFF8F0`
- **主色**：暖橘 `#FF8C42`（按钮、发送、选中态）
- **辅助色**：薄荷绿 `#7BC8A4`（达标/正向）、天空蓝 `#6BB6E8`（信息/图表）、柠檬黄 `#FFD166`（预警）、珊瑚红 `#FF6B6B`（超支/警告）
- **文字色**：深棕灰 `#4A3F35`（主文字）、浅棕灰 `#9B8E82`（次要文字）
- **圆角**：卡片 16px、聊天气泡 20px、按钮 12px
- **阴影**：极柔和 `0 2px 12px rgba(255,140,66,0.08)`
- **宠物形象**：emoji 实现，5 级进化形态
- **整体调性**：大量留白、圆角卡片、柔和配色，像手账而非工具

---

## 2. 整体页面架构和导航结构

### 2.1 底部导航栏
固定底部，5 个 Tab，选中项暖橘色，未选中灰色：

| 序号 | 图标 | Tab 名称 | 核心功能 |
|---|---|---|---|
| 1 | 🏠 | 记账 | 聊天式记账主界面 |
| 2 | 📊 | 概览 | 今日/本月数据、快捷记账、统计图表 |
| 3 | 📝 | 账单 | 历史记录列表、详情、删除、备注 |
| 4 | 🐱 | 宠物 | 宠物养成、连续打卡、徽章墙、省钱挑战、存钱罐、账单壁纸 |
| 5 | ⚙️ | 我的 | 预算管理、分类管理、心情设置、宠物设置、数据管理、关于 |

### 2.2 页面层级图

```
APP 启动
├── Tab 1: 记账（聊天首页）
│   └── （无下级页面，所有交互在当前页完成）
│
├── Tab 2: 概览
│   ├── 快捷记账按钮 → 跳转 Tab 1 并自动填入分类关键词
│   ├── 宠物小卡片 → 跳转 Tab 4
│   ├── 最近消费记录 → 跳转 Tab 3 记录详情页
│   └── 图表区（近7天走势 / 分类占比 / 月度趋势，左右滑动切换）
│
├── Tab 3: 账单
│   ├── 月份选择器（左右切换）
│   ├── 分类筛选
│   ├── 记录列表（按日期分组）
│   └── 记录详情页（点击单条记录进入）
│       ├── 备注编辑
│       ├── 心情标签修改
│       └── 删除记录
│
├── Tab 4: 宠物
│   ├── 宠物展示区（形象/等级/经验/心情，点击互动）
│   ├── 连续打卡区（火焰/天数/本月日历）
│   ├── 成就徽章墙（点击查看徽章详情弹窗）
│   ├── 省钱挑战区（进行中 / 更多挑战列表 / 挑战详情）
│   ├── 存钱罐区（目标/进度/存入/设置目标）
│   └── 账单壁纸生成（选月份 → 预览 → 保存 PNG）
│
└── Tab 5: 我的
    ├── 预算管理（设置金额/进度/提醒开关）
    ├── 分类管理（列表/新增/编辑/删除）
    ├── 心情标签设置（开关）
    ├── 宠物设置（改名/重置）
    ├── 数据管理（导出备份/导入备份/清空数据）
    └── 关于（版本号/设计说明）
```

---

## 3. 首页（记账 Tab）的内容结构

### 3.1 页面定位
首页是聊天式记账界面，类似微信聊天窗口，是用户最频繁使用的页面。

### 3.2 顶部栏
- 左侧：小猫宠物头像（emoji）+ 宠物名字 + 心情 emoji（点击跳转到宠物 Tab）
- 右侧：今日已花金额（小字）
- 样式：奶油白背景，底部细分割线

### 3.3 聊天消息区
- 占满中间区域，可上下滚动，最新消息在底部
- **右侧气泡**（用户消息）：暖橘渐变背景 + 白色文字，显示用户输入的原始内容，如"吃饭 25 元"
- **左侧气泡**（系统/宠物消息）：白色背景 + 浅描边 + 小猫头像，显示系统回复
  - 记账成功："已记录：餐饮 25 元，时间 12:30 🐱"
  - 识别失败："没看懂哦，试试这样输入：吃饭 25 元 / 奶茶 15"
  - 预算提醒：达到阈值时的提示消息
  - 升级/徽章解锁通知
- 消息时间显示在系统回复中（HH:MM 格式）

### 3.4 底部输入区
- **快捷分类标签栏**：横向滚动，7 个预设分类（餐饮🍚、学习用品📚、生活用品🧴、交通🚌、娱乐🎮、购物🛍️、其他📦），点击自动在输入框填入分类名；上次使用的分类高亮显示
- **输入框**：白色圆角，占位文字"记一笔，如：吃饭 25 元"
- **发送按钮**：暖橘圆形，点击或回车发送
- 输入区固定在底部，不随消息滚动

### 3.5 记账触发的联动逻辑
每成功记一笔账，依次执行以下检查：
1. 保存消费记录到 LocalStorage
2. 宠物获得经验，检查是否升级（升级时弹动画通知）
3. 计算本月预算使用比例，检查是否触发五级提醒
4. 更新连续打卡（今天第一笔记账时，连续天数 +1）
5. 检查所有徽章解锁条件，满足则解锁并通知
6. 检查进行中的省钱挑战是否被违反（如记了"奶茶"则"不喝奶茶"挑战失败）
7. 刷新所有页面的显示数据

---

## 4. 每个模块的内容

### 4.1 聊天式记账（首页）
见第 3 节。

### 4.2 今日概览（概览 Tab）
从上到下滚动布局：

1. **今日总开销**：大字号金额 + 今日记账笔数
2. **预算卡片**：本月已花金额 / 剩余预算金额 / 进度条；进度条颜色随预算使用比例变化（<50% 绿、50-80% 黄、80-90% 橙、90-100% 红、超支深红）；点击跳转到"我的-预算管理"
3. **快捷记账区**：6 个常用分类大按钮（带 emoji），点击 → 切换到记账 Tab 并自动填入该分类名，用户只需补金额
4. **宠物小卡片**：小猫形象 + 等级 + 心情一句话描述，点击跳转到宠物 Tab
5. **最近消费**：最近 5 笔记录列表，每条显示分类 emoji + 分类名 + 金额 + 时间，点击跳转到账单详情页
6. **图表区**：左右滑动切换 3 张图表
   - 近 7 天花销走势（柱状图，每天一根柱）
   - 本月各分类消费占比（环形图，图例显示分类名和金额）
   - 近 6 个月消费趋势（柱状图，点击某月可查看该月明细）

### 4.3 开销记录（账单 Tab）
- **顶部**：月份选择器（左右箭头切换月份）+ 当月总开销金额 + 分类筛选按钮（可按分类筛选记录）
- **记录列表**：按日期分组，每组顶部显示日期 + 当日小计金额；每条记录显示分类 emoji + 分类名 + 金额（右侧红色）+ 时间 + 心情小标签（如有）+ 备注小图标（如有备注）
- **记录详情页**（点击单条记录进入）：
  - 大字号显示金额
  - 分类、发生时间（日期 + 具体时间）
  - 备注：可编辑文本框，保存后更新
  - 心情标签：4 个按钮切换（开心😊 / 一般😐 / 难过😢 / 冲动😤）
  - 底部删除按钮（红色，点击后二次确认弹窗）

### 4.4 分类管理（我的 Tab 内）
- 分类列表：显示所有分类（7 个预设 + 用户自定义），每行显示 emoji 图标 + 分类名 + 编辑/删除按钮
- **新增分类**：弹窗输入分类名称 + 选择 emoji 图标，保存后加入列表
- **编辑分类**：修改名称或更换 emoji 图标
- **删除分类**：如果该分类下已有消费记录，提示"该分类下有 N 条记录，删除后这些记录将归为'其他'分类，确认删除？"；无记录则直接删除
- 预设分类（不可删除，可编辑名称和图标）：餐饮🍚、学习用品📚、生活用品🧴、交通🚌、娱乐🎮、购物🛍️、其他📦

### 4.5 预算监控（我的 Tab 内设置 + 全局提醒）
- **预算设置**：月度总预算金额，默认 1200 元，可修改
- **预算进度展示**：本月已用金额、剩余金额、使用比例、进度条
- **五级提醒**（每笔记账后检查）：

| 比例 | 触发时机 | 表现形式 |
|---|---|---|
| 50% | 本月首次达到 | 左侧气泡温和提示"已用一半，继续保持 👍" |
| 80% | 本月首次达到 | 记账后弹黄色预警弹窗"预算已用 80%，注意控制哦" |
| 90% | 每次记账时 | 橙色警告 + 二次确认弹窗"预算已用 90%，确定要记这笔吗？" |
| 100% | 本月首次达到 | 红色警报 + 全屏撒花动画 + "精准达标！本月预算刚好用完 🎉" |
| 超支 | 每次记账时 | 宠物心情变难过 😿 + 左侧气泡超支分析（超支金额、花销最多的分类、控制建议） |

- 提醒开关：可在设置中开启/关闭预算提醒
- 每月 1 日自动重置本月已提醒状态（各阈值重新触发）

### 4.6 月度统计（概览 Tab 图表区 + 账单 Tab 月份切换）
- 切换月份查看：每月总开销、各分类花销金额及占比
- 近 6 个月消费趋势柱状图
- 月度数据包含：总金额、记账笔数、日均开销、最大一笔、分类排名

### 4.7 消费备注（账单详情页内）
- 每条记录可添加/修改文字备注
- 备注在列表中以小图标标识，在详情页完整显示
- 备注无长度限制（实际受 LocalStorage 容量限制）

### 4.8 统计图表（概览 Tab 图表区）
- 近 7 天花销走势：柱状图，X 轴日期，Y 轴金额
- 各分类消费占比：环形图，不同分类用不同柔和配色，图例显示金额和百分比
- 月度消费趋势：近 6 个月柱状图，可点击查看单月明细
- 图表样式：圆角柱、柔和配色、无多余网格线，与整体温暖风格一致

### 4.9 个人基础设置（我的 Tab）
- 预算管理（见 4.5）
- 分类管理（见 4.4）
- 心情标签开关：开启后记账可添加心情标签
- 宠物设置：宠物改名、重置宠物（清零等级经验，二次确认）
- 关于：版本号、设计说明

### 4.10 数据管理（我的 Tab 内）
- **导出备份**：将所有 LocalStorage 数据打包为 JSON 文件，触发手机下载（文件名格式：记账备份_YYYYMMDD_HHMMSS.json）
- **导入备份**：选择本地 JSON 文件，解析后覆盖当前所有数据（导入前二次确认"将覆盖当前所有数据，确认导入？"）
- **清空全部数据**：二次确认 + 输入"确认"后清空所有 LocalStorage 数据，恢复到初始状态

### 4.11 记账虚拟宠物（宠物 Tab）
- **5 级进化形态**（emoji）：🥚 猫蛋 → 🐱 小奶猫 → 😺 幼猫 → 😻 成年猫 → 👑 猫王
- **经验系统**：每记一笔账获得经验（基础 +10，金额越大经验越多，公式：10 + 金额/10 取整），经验满后升级
- **等级进度条**：显示当前经验 / 下一级所需经验
- **心情系统**：心情与本月预算使用比例联动

| 预算使用比例 | 心情 | emoji | 描述 |
|---|---|---|---|
| < 50% | 开心 | 😺 | 状态很好，继续保持 |
| 50% ~ 80% | 一般 | 😐 | 还算正常，注意控制 |
| 80% ~ 90% | 紧张 | 😰 | 有点紧张了，少花点 |
| 90% ~ 100% | 焦虑 | 😱 | 快超支了，住手！ |
| 超支 | 难过 | 😿 | 超支了，下个月加油 |

- **互动**：点击宠物形象，随机弹出一句宠物台词 + 轻微弹跳动画
- **宠物名字**：默认"小橘"，可在设置中修改

### 4.12 成就徽章系统（宠物 Tab）
- 约 20 个徽章，分 4 类：
  - **记账坚持类**：首笔记账、连续 7 天、连续 30 天、连续 100 天、累计 100 笔、累计 500 笔、累计 1000 笔、单日 ≥5 笔
  - **预算管理类**：月度达标（不超支）、连续 3 月达标、零超支月、精准 100% 达标
  - **分类达人类**：用齐 7 个分类、单分类累计 50 笔
  - **趣味彩蛋类**：深夜记账（23:00 后）、凌晨记账（05:00 前）、宠物满级、冲动消费 ≥3 次
- **徽章墙展示**：已解锁徽章彩色显示，未解锁灰色显示；按分类分组
- **徽章详情**：点击徽章弹出详情，显示名称、描述、解锁条件、解锁时间
- **解锁通知**：满足条件时立即弹出徽章解锁动画 + 通知消息

### 4.13 连续记账打卡（宠物 Tab）
- **打卡规则**：每天记第一笔账时自动打卡，连续天数 +1；如果昨天没有记账记录，连续天数重置为 1
- **火焰标识**：显示当前连续天数，火焰颜色随天数变化
  - 1~6 天：普通火焰 🔥
  - 7~29 天：蓝色火焰 💎，称号"记账新手"
  - 30~99 天：紫色火焰 👑，称号"记账达人"
  - 100 天+：金色火焰 ✨，称号"记账之神"
- **本月打卡日历**：小日历视图，已记账的日期标记圆点

### 4.14 省钱挑战（宠物 Tab）
- **预设挑战**：
  - 30 天不喝奶茶（记账中出现"奶茶/饮料"分类则失败）
  - 一周低碳出行（7 天内"交通"分类 ≤2 笔）
  - 7 天不购物（7 天内"购物"分类 0 笔）
  - 30 天餐饮 ≤600 元（30 天内"餐饮"分类累计 ≤600）
- **挑战流程**：浏览挑战列表 → 加入挑战（记录开始日期）→ 系统自动根据记账记录判断进度/失败 → 挑战成功奖励经验 + 徽章
- **进行中展示**：挑战名称、进度条、剩余天数、当前状态
- **失败处理**：违反挑战规则时标记失败，可重新加入
- **挑战详情**：规则说明、奖励、当前进度

### 4.15 存钱罐目标（宠物 Tab）
- **目标设定**：设置存钱目标金额（如 500 元），记录开始日期
- **存入操作**：手动输入金额存入存钱罐，存入金额从可支配金额中扣除（仅记录，不影响预算统计）
- **进度展示**：小猪 emoji 🐷 + 当前金额 / 目标金额 + 进度条；小猪形象随进度变化（空罐 → 半满 → 满罐）
- **达成庆祝**：达到目标金额时播放庆祝动画
- **取出/重置**：可取出部分金额，或重置存钱罐（清零，二次确认）

### 4.16 账单壁纸生成（宠物 Tab）
- **选择月份**：选择要生成壁纸的月份
- **壁纸内容**：月度总开销、记账笔数、分类占比小环形图、最大一笔消费、记账天数、宠物形象 + 等级、一句金句（如"每一笔都算数"）
- **壁纸样式**：奶油白背景 + 暖橘装饰元素 + 圆角卡片排版，分辨率适配手机屏幕（1080×1920 或按设备屏幕比例）
- **预览**：生成后显示预览图
- **保存**：点击"保存到手机"，将 Canvas 导出为 PNG 图片并触发下载

### 4.17 消费心情标签（账单详情页 + 月度统计）
- **4 种心情**：开心😊、一般😐、难过😢、冲动😤
- **添加方式**：在账单详情页点击心情按钮为该条记录标记心情；不强制，默认为空
- **统计分析**：月度统计中显示各心情标签的消费金额和笔数，重点突出"冲动"消费的总额和占比，帮助用户反思

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 消费记录（records）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 唯一标识（时间戳 + 随机数） |
| amount | number | 金额（元，支持小数） |
| categoryId | string | 分类 ID |
| categoryName | string | 分类名称（冗余存储，避免分类改名后历史记录变化） |
| categoryIcon | string | 分类 emoji（冗余存储） |
| note | string | 备注（可为空） |
| mood | string | 心情标签（happy/normal/sad/impulse，可为空） |
| timestamp | number | 记录时间戳（毫秒） |
| date | string | 日期字符串（YYYY-MM-DD，用于按天分组查询） |
| time | string | 时间字符串（HH:MM，用于显示） |

**必要操作**：新增、按日期/月份查询、按分类筛选、单条删除、修改备注、修改心情标签。

### 5.2 分类（categories）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 唯一标识 |
| name | string | 分类名称 |
| icon | string | emoji 图标 |
| isDefault | boolean | 是否预设分类（预设不可删除） |
| sort | number | 排序序号 |

**必要操作**：新增、编辑（改名/换图标）、删除（有记录时归为"其他"）、查询全部、按使用频率排序。

### 5.3 预算（budget）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| monthlyAmount | number | 月度预算金额（默认 1200） |
| reminderEnabled | boolean | 是否开启预算提醒 |
| notified | object | 本月各阈值是否已提醒（{50:false, 80:false, 90:false, 100:false}） |
| notifyMonth | string | 已提醒状态所属月份（YYYY-MM，跨月自动重置） |

**必要操作**：修改预算金额、开关提醒、查询本月已用/剩余/比例、标记阈值已提醒、跨月重置提醒状态。

### 5.4 宠物（pet）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 宠物名字（默认"小橘"） |
| level | number | 等级（1~5） |
| exp | number | 当前经验 |
| totalExp | number | 累计总经验 |
| mood | string | 当前心情（由预算比例计算，不存储，实时计算） |
| lastInteract | number | 上次互动时间戳 |

**必要操作**：增加经验、检查升级、修改名字、重置（清零）、查询等级进度。

**等级经验表**：
| 等级 | 形态 | 升级所需经验 | 累计经验 |
|---|---|---|---|
| 1 | 🥚 猫蛋 | 50 | 0 |
| 2 | 🐱 小奶猫 | 150 | 50 |
| 3 | 😺 幼猫 | 300 | 200 |
| 4 | 😻 成年猫 | 600 | 500 |
| 5 | 👑 猫王 | （满级） | 1100 |

### 5.5 徽章（badges）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 徽章唯一标识 |
| name | string | 徽章名称 |
| desc | string | 徽章描述 |
| icon | string | emoji 图标 |
| category | string | 分类（persistence/budget/category/fun） |
| unlocked | boolean | 是否已解锁 |
| unlockedAt | number | 解锁时间戳（未解锁为 null） |
| condition | string | 解锁条件描述 |

**必要操作**：查询全部（按分类分组）、检查解锁条件、标记解锁、查询已解锁数量。

### 5.6 打卡（checkin）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| continuousDays | number | 当前连续天数 |
| lastCheckinDate | string | 上次打卡日期（YYYY-MM-DD） |
| totalDays | number | 累计打卡天数 |
| checkinDates | array | 本月打卡日期列表（用于日历展示） |

**必要操作**：每日首笔记账时打卡、计算连续天数（判断昨天是否有记录）、查询本月打卡日历、跨月重置日历。

### 5.7 省钱挑战（challenges）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 挑战唯一标识 |
| name | string | 挑战名称 |
| desc | string | 挑战描述 |
| type | string | 挑战类型（no_category/category_limit/amount_limit） |
| targetCategory | string | 目标分类 ID（类型相关） |
| targetValue | number | 目标值（天数/金额/笔数） |
| duration | number | 持续天数 |
| startDate | string | 开始日期（YYYY-MM-DD） |
| status | string | 状态（in_progress/success/failed） |
| progress | number | 当前进度值 |

**必要操作**：加入挑战（创建记录）、每笔记账后检查进行中挑战的状态/进度、查询进行中和已完成挑战、标记成功/失败。

### 5.8 存钱罐（piggyBank）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| targetAmount | number | 目标金额 |
| currentAmount | number | 当前已存金额 |
| startDate | string | 开始日期 |
| achieved | boolean | 是否已达成 |
| achievedAt | number | 达成时间戳 |

**必要操作**：设置目标、存入金额、取出金额、查询进度、重置、达成检测。

### 5.9 设置（settings）
**必要数据字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| moodEnabled | boolean | 是否开启心情标签（默认 true） |
| budgetReminderEnabled | boolean | 是否开启预算提醒（默认 true） |
| lastCategoryId | string | 上次使用的分类 ID（用于快捷分类高亮） |

**必要操作**：修改设置项、查询设置项。

---

## 6. 首页、今日概览和各模块之间的关系

### 6.1 页面跳转关系

| 来源页面 | 触发操作 | 目标页面 | 传递数据 |
|---|---|---|---|
| 概览 Tab | 点击快捷记账分类按钮 | 记账 Tab | 自动填入分类名到输入框 |
| 概览 Tab | 点击宠物小卡片 | 宠物 Tab | 无 |
| 概览 Tab | 点击最近消费记录 | 账单详情页 | 记录 ID |
| 概览 Tab | 点击预算卡片 | 我的 Tab-预算管理 | 无 |
| 记账 Tab | 点击顶部宠物头像 | 宠物 Tab | 无 |
| 账单 Tab | 点击单条记录 | 账单详情页 | 记录 ID |
| 宠物 Tab | 点击宠物互动 | 当前页动画 | 无 |
| 宠物 Tab | 点击徽章 | 徽章详情弹窗 | 徽章 ID |
| 我的 Tab | 点击分类管理 | 分类管理页 | 无 |
| 我的 Tab | 点击数据管理 | 数据管理页 | 无 |

### 6.2 数据流转关系

```
用户输入一句话（记账 Tab）
    │
    ▼
解析分类 + 金额 → 保存记录（records）
    │
    ├──→ 更新今日概览数据（概览 Tab）
    ├──→ 更新账单列表（账单 Tab）
    ├──→ 宠物 +经验 → 检查升级（宠物 Tab）
    ├──→ 计算预算比例 → 触发五级提醒（全局）
    ├──→ 更新连续打卡（宠物 Tab）
    ├──→ 检查徽章解锁条件（宠物 Tab）
    ├──→ 检查省钱挑战状态（宠物 Tab）
    └──→ 更新月度统计/图表（概览 Tab）
```

### 6.3 各模块数据依赖

| 模块 | 依赖的数据 | 被谁依赖 |
|---|---|---|
| 消费记录 | 分类（解析时匹配） | 概览、账单、月度统计、图表、预算、宠物经验、打卡、徽章、挑战 |
| 分类 | 无 | 消费记录解析、分类管理、统计图表 |
| 预算 | 消费记录（计算已用） | 概览进度条、宠物心情、五级提醒 |
| 宠物 | 消费记录（经验来源）、预算（心情来源） | 首页顶部头像、概览小卡片、宠物 Tab |
| 徽章 | 消费记录、打卡、预算、宠物、挑战 | 宠物 Tab 徽章墙 |
| 打卡 | 消费记录（每日首笔） | 宠物 Tab、徽章 |
| 挑战 | 消费记录（判断违反/进度） | 宠物 Tab、徽章 |
| 存钱罐 | 无（独立手动操作） | 宠物 Tab |
| 设置 | 无 | 全局行为控制 |

### 6.4 首页与概览的协作
- **首页（记账 Tab）** 是数据入口，负责快速录入
- **概览 Tab** 是数据出口，负责展示录入后的统计结果
- 两者通过"快捷记账按钮"双向连接：概览页点快捷按钮 → 跳首页并预填分类；首页记完账 → 概览数据自动更新
- 首页顶部宠物头像和概览页宠物小卡片都指向宠物 Tab，形成"记账→看宠物→再记账"的循环

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 数据存储方式
- 所有数据存储在 WebView 的 **LocalStorage** 中
- 存储 Key：`accounting_app_data`（单个 JSON 对象包含所有数据）
- 数据结构：

```json
{
  "records": [],
  "categories": [],
  "budget": {},
  "pet": {},
  "badges": [],
  "checkin": {},
  "challenges": [],
  "piggyBank": {},
  "settings": {},
  "version": "1.0"
}
```

### 7.2 数据持久化要求
- ✅ 刷新页面后数据不丢失
- ✅ 关闭 APP 后重新打开数据不丢失
- ✅ 重启手机后打开 APP 数据不丢失
- ✅ 离线状态下所有功能正常使用（数据读写不依赖网络）
- ⚠️ 清除 APP 数据/缓存会导致数据丢失（需提前导出备份）
- ⚠️ 卸载 APP 会导致数据丢失（需提前导出备份）

### 7.3 导出备份
- **操作入口**：我的 Tab → 数据管理 → 导出备份
- **导出格式**：JSON 文件，文件名格式 `记账备份_YYYYMMDD_HHMMSS.json`
- **导出内容**：完整的 LocalStorage 数据对象（包含所有模块数据）
- **触发方式**：通过 Capacitor 的 Filesystem 或浏览器下载机制保存到手机存储
- **导出后提示**："备份已保存到手机，文件名：xxx.json"

### 7.4 导入备份
- **操作入口**：我的 Tab → 数据管理 → 导入备份
- **导入方式**：选择本地 JSON 文件
- **导入流程**：
  1. 选择文件
  2. 解析 JSON，验证数据格式（必须包含 records、categories 等核心字段）
  3. 弹出二次确认："将覆盖当前所有数据，确认导入？导入后当前数据将无法恢复。"
  4. 确认后用导入数据覆盖 LocalStorage
  5. 刷新所有页面数据
- **导入失败处理**：文件格式错误时提示"备份文件格式不正确，请选择有效的备份文件"

### 7.5 清空数据
- **操作入口**：我的 Tab → 数据管理 → 清空全部数据
- **清空流程**：
  1. 点击清空按钮
  2. 弹出确认弹窗："确定要清空所有数据吗？此操作不可恢复！"
  3. 要求输入"确认"二字（防止误操作）
  4. 清空 LocalStorage，恢复到初始状态（重新初始化预设分类、默认预算、初始宠物等）
  5. 跳转到记账 Tab，显示欢迎消息

### 7.6 数据初始化
- 首次启动 APP 时（LocalStorage 无数据），自动初始化：
  - 7 个预设分类
  - 默认预算 1200 元
  - 初始宠物（等级 1，名字"小橘"，经验 0）
  - 约 20 个徽章（全部未解锁）
  - 默认设置
  - 记账 Tab 显示欢迎消息："你好呀！我是小橘，以后陪你一起记账～试试输入：吃饭 25 元"

---

## 8. 第一版必须完成的功能

### 8.1 基础功能（10 个模块全部完成）

| 序号 | 模块 | 必须完成的功能 |
|---|---|---|
| 1 | 聊天式记账首页 | 一句话输入识别分类+金额、左右气泡、识别失败提示、快捷分类标签、记账后系统确认消息 |
| 2 | 今日概览 | 今日总开销、本月已花/剩余预算、预算进度条、快捷记账按钮（跳转首页预填分类）、最近5笔消费、3张图表（近7天走势/分类占比/月度趋势） |
| 3 | 开销记录 | 按月份切换、按日期分组列表、当日小计、记录详情页、单条删除 |
| 4 | 分类管理 | 7个预设分类、新增分类（名称+emoji）、编辑分类、删除分类（有记录归为其他） |
| 5 | 预算监控 | 默认1200元、已用/剩余展示、五级提醒（50%/80%/90%/100%/超支）、提醒开关、跨月重置 |
| 6 | 月度统计 | 每月总开销、各分类花销、月份切换、近6个月趋势 |
| 7 | 消费备注 | 单条记录添加/修改备注、列表备注标识 |
| 8 | 统计图表 | 近7天柱状图、分类环形图、月度趋势柱状图 |
| 9 | 个人基础设置 | 预算修改、分类管理入口、心情开关、宠物改名/重置 |
| 10 | 数据管理 | 导出备份JSON、导入备份JSON、清空全部数据 |

### 8.2 趣味功能（7 个全部完成）

| 序号 | 功能 | 必须完成的内容 |
|---|---|---|
| 1 | 记账虚拟宠物 | 5级进化形态、经验系统、等级进度条、心情与预算联动、点击互动、宠物改名 |
| 2 | 成就徽章系统 | 约20个徽章、4类分组、徽章墙展示、徽章详情弹窗、解锁动画通知 |
| 3 | 连续记账打卡 | 每日首笔自动打卡、连续天数计算、火焰图标（7/30/100天变色+称号）、本月打卡日历 |
| 4 | 省钱挑战 | 4个预设挑战、加入挑战、自动判断进度/失败、挑战成功奖励、进行中展示 |
| 5 | 存钱罐目标 | 设定目标金额、手动存入/取出、进度条、小猪形象随进度变化、达成庆祝、重置 |
| 6 | 账单壁纸生成 | 选择月份、生成精美壁纸（含月度数据+宠物形象）、预览、保存PNG到手机 |
| 7 | 消费心情标签 | 4种心情、账单详情页标记、月度统计中冲动消费分析 |

### 8.3 技术要求（必须完成）
- ✅ 单个 HTML 文件（内联 CSS、JS）作为 Web 层
- ✅ Capacitor 项目结构完整（www 目录、capacitor.config.json、android 项目）
- ✅ 可编译生成 APK 文件
- ✅ 所有数据 LocalStorage 持久化
- ✅ 离线可用（不依赖网络）
- ✅ 全屏运行（添加到桌面后独立图标）
- ✅ 移动端适配，最大宽度 520px
- ✅ 底部固定导航栏（5 个 Tab）
- ✅ 聊天窗口左右气泡样式
- ✅ 温暖治愈视觉风格（奶油白+暖橘+圆角+小猫emoji）
- ✅ 预算 100% 时全屏撒花动画

---

## 9. 第一版暂时不做的功能

| 功能 | 不做原因 | 可能的第二版方案 |
|---|---|---|
| 收入记录 | 第一版定位为纯开销记账，不涉及收入管理 | 增加收入模块，支持收入分类和收支对比 |
| 多账本/多账户 | 只供个人使用，单账本足够 | 支持多个账本（如日常/旅行/装修） |
|  recurring 周期性记账（自动重复） | 第一版手动记账，周期账单场景较少 | 支持设置每月固定支出（房租/订阅）自动入账 |
| 数据云同步 | 明确不需要联网、云数据库 | 如未来需要，可接入 WebDAV 或本地网络同步 |
| 多用户/多账号 | 只供个人使用 | 不考虑 |
| 消息推送提醒 | 明确不需要推送 | 不考虑（本地通知可作为第二版选项） |
| 预算按分类设置（每分类独立预算） | 第一版只有月度总预算 | 支持为每个分类设置独立预算和提醒 |
| 账单搜索（按关键词/金额范围） | 第一版按日期+分类筛选足够 | 增加全局搜索功能 |
| 数据导出为 Excel/CSV | 第一版只导出 JSON 备份 | 支持导出 CSV/Excel 格式 |
| 宠物更多互动（喂食/抚摸/小游戏） | 第一版只有点击互动台词 | 增加喂食、抚摸、小游戏等深度互动 |
| 自定义壁纸模板 | 第一版只有固定壁纸样式 | 支持多种壁纸模板和自定义配色 |
| 暗黑模式 | 第一版只有温暖浅色主题 | 增加暗黑模式切换 |
| 手势操作（左滑删除等） | 第一版通过详情页删除，降低实现复杂度 | 增加列表左滑删除、下拉刷新等手势 |
| 桌面小组件（Widget） | Capacitor 原生 Widget 开发复杂度高 | 第二版研究实现安卓桌面小组件 |

---

## 10. 可以实际检查的验收标准

### 10.1 安装与启动
- [ ] APK 文件可在安卓手机上正常安装（允许未知来源后）
- [ ] 安装后桌面出现 APP 图标（小猫图标 + 应用名称）
- [ ] 点击图标可正常启动，全屏显示，无浏览器地址栏
- [ ] 首次启动显示初始化欢迎消息，预设 7 个分类已存在
- [ ] 关闭 APP 后从最近任务重新打开，数据不丢失
- [ ] 重启手机后打开 APP，数据不丢失
- [ ] 开启飞行模式（完全离线）下，所有功能正常使用

### 10.2 聊天式记账
- [ ] 在输入框输入"吃饭 25 元"，右侧气泡显示输入内容，左侧气泡回复"已记录：餐饮 25 元，时间 HH:MM"
- [ ] 输入"学习用品 38 元"，正确识别为"学习用品"分类
- [ ] 输入"奶茶 15"（无"元"字），能正确识别金额 15
- [ ] 输入"今天花了好多钱"（无金额无分类），左侧气泡回复识别失败提示并给出输入示例
- [ ] 点击快捷分类标签（如"餐饮"），输入框自动填入"餐饮"
- [ ] 上次使用的分类在快捷标签栏高亮显示
- [ ] 记账后消息自动滚动到底部，显示最新消息
- [ ] 左右气泡样式正确区分（右侧暖橘渐变，左侧白底+小猫头像）

### 10.3 今日概览
- [ ] 今日总开销金额等于今天所有记录的金额之和
- [ ] 本月已花金额等于本月所有记录金额之和
- [ ] 剩余预算 = 月度预算 - 本月已花
- [ ] 预算进度条颜色随使用比例变化（<50%绿、50-80%黄、80-90%橙、>90%红）
- [ ] 点击快捷记账按钮（如"餐饮"），跳转到记账 Tab 且输入框已填入"餐饮"
- [ ] 最近消费列表显示最近 5 笔记录，点击可跳转到账单详情
- [ ] 近 7 天走势图正确显示每天金额
- [ ] 分类占比环形图正确显示各分类金额和百分比
- [ ] 月度趋势图显示近 6 个月数据

### 10.4 开销记录与账单
- [ ] 账单列表按日期分组，每组显示日期和当日小计
- [ ] 左右箭头可切换月份，数据对应变化
- [ ] 分类筛选可只显示某分类的记录
- [ ] 点击单条记录进入详情页，显示金额、分类、时间
- [ ] 在详情页修改备注，保存后列表显示备注图标
- [ ] 在详情页切换心情标签，保存后正确显示
- [ ] 点击删除按钮，弹出二次确认，确认后记录从列表消失
- [ ] 删除记录后，今日概览/月度统计金额相应减少

### 10.5 分类管理
- [ ] 7 个预设分类存在且不可删除
- [ ] 新增分类（名称+emoji）后出现在列表和快捷标签栏
- [ ] 编辑分类名称后，已有记录的分类名不变（冗余存储）
- [ ] 删除有记录的分类时，提示记录将归为"其他"，确认后记录分类变为"其他"
- [ ] 删除无记录的分类时直接删除

### 10.6 预算监控与五级提醒
- [ ] 默认月度预算为 1200 元，可在设置中修改
- [ ] 本月已用达到 50%（600元）时，左侧气泡出现温和提示（本月首次）
- [ ] 本月已用达到 80%（960元）时，记账后弹出黄色预警弹窗（本月首次）
- [ ] 本月已用达到 90%（1080元）时，每次记账弹出橙色警告+二次确认
- [ ] 本月已用达到 100%（1200元）时，出现红色警报+全屏撒花动画（本月首次）
- [ ] 超支后，宠物心情变为难过 😿，左侧气泡给出超支分析
- [ ] 关闭预算提醒开关后，不再触发提醒弹窗
- [ ] 跨月后（如从9月到10月），所有阈值提醒状态重置，可重新触发

### 10.7 记账虚拟宠物
- [ ] 宠物初始等级为 1（🥚 猫蛋），名字默认"小橘"
- [ ] 每记一笔账，宠物经验增加，进度条更新
- [ ] 经验达到升级阈值时，弹出升级动画，形态进化（🥚→🐱→😺→😻→👑）
- [ ] 预算使用 <50% 时宠物心情为开心 😺，>90% 时为焦虑 😱，超支为难过 😿
- [ ] 点击宠物形象，弹出随机台词并有弹跳动画
- [ ] 在设置中可修改宠物名字
- [ ] 重置宠物后，等级和经验清零，恢复为等级 1

### 10.8 成就徽章系统
- [ ] 徽章墙显示约 20 个徽章，按 4 类分组
- [ ] 未解锁徽章为灰色，已解锁为彩色
- [ ] 首笔记账后解锁"首笔记账"徽章，弹出解锁动画
- [ ] 连续记账 7 天后解锁对应徽章
- [ ] 点击徽章弹出详情（名称、描述、解锁条件、解锁时间）
- [ ] 预算精准达标（刚好用完1200）解锁对应彩蛋徽章

### 10.9 连续记账打卡
- [ ] 每天第一笔记账后，连续天数 +1
- [ ] 昨天有记账记录时，今天记账连续天数递增
- [ ] 昨天无记账记录时，今天记账连续天数重置为 1
- [ ] 连续 7 天火焰变蓝色，称号"记账新手"
- [ ] 连续 30 天火焰变紫色，称号"记账达人"
- [ ] 连续 100 天火焰变金色，称号"记账之神"
- [ ] 本月打卡日历正确标记已记账日期

### 10.10 省钱挑战
- [ ] 挑战列表显示 4 个预设挑战
- [ ] 加入挑战后，挑战状态变为"进行中"，记录开始日期
- [ ] 进行"30天不喝奶茶"挑战时，记一笔"奶茶"分类记录，挑战状态变为"失败"
- [ ] 挑战成功后状态变为"成功"，奖励宠物经验
- [ ] 进行中挑战显示进度条和剩余天数

### 10.11 存钱罐
- [ ] 可设置存钱目标金额
- [ ] 存入金额后，当前金额和进度条更新
- [ ] 小猪形象随进度变化（空→半满→满）
- [ ] 达到目标金额时播放庆祝动画
- [ ] 可取出金额，可重置存钱罐

### 10.12 账单壁纸生成
- [ ] 可选择月份生成壁纸
- [ ] 壁纸包含月度总开销、记账笔数、分类占比小图、宠物形象
- [ ] 壁纸预览正确显示
- [ ] 点击保存后，PNG 图片下载到手机

### 10.13 消费心情标签
- [ ] 在账单详情页可标记 4 种心情
- [ ] 标记后列表显示心情小标签
- [ ] 月度统计中显示各心情消费金额，冲动消费单独突出

### 10.14 数据管理
- [ ] 导出备份生成 JSON 文件，文件名包含日期时间
- [ ] 导出的 JSON 文件包含所有数据（记录、分类、预算、宠物、徽章等）
- [ ] 导入备份时，选择有效 JSON 文件，二次确认后数据被覆盖
- [ ] 导入无效文件时提示格式错误
- [ ] 清空数据需输入"确认"二字，清空后恢复初始状态
- [ ] 清空后所有记录、宠物等级、徽章等全部重置

### 10.15 视觉与交互
- [ ] 整体配色为奶油白背景 + 暖橘主色，符合设计风格
- [ ] 所有卡片、按钮、气泡为圆角设计
- [ ] 最大宽度 520px，在宽屏手机上居中显示
- [ ] 底部导航栏固定，5 个 Tab 切换流畅无卡顿
- [ ] 页面切换有过渡动画
- [ ] 所有文字清晰可读，无溢出、无重叠
- [ ] 预算 100% 时全屏撒花动画正常播放

---

## 附录：关键默认值

| 项目 | 默认值 |
|---|---|
| 月度预算 | 1200 元 |
| 预设分类 | 餐饮🍚、学习用品📚、生活用品🧴、交通🚌、娱乐🎮、购物🛍️、其他📦 |
| 宠物名字 | 小橘 |
| 宠物初始等级 | 1（🥚 猫蛋） |
| 每笔经验 | 10 + 金额/10（取整） |
| 心情标签 | 开心😊、一般😐、难过😢、冲动😤 |
| 预算阈值 | 50%、80%、90%、100% |
| 打卡火焰阶段 | 7天（蓝）、30天（紫）、100天（金） |
| 预设挑战 | 30天不喝奶茶、一周低碳出行、7天不购物、30天餐饮≤600 |
| 页面最大宽度 | 520px |
| 存储 Key | accounting_app_data |
| 数据版本 | 1.0 |

---

> 本文档为第一版开发的唯一依据。如需变更，需更新本文档并重新确认。
